// Retrieve all documents from the 'school' collection with name = "Holberton school"
var documents = db.school.find({ name: "Holberton school" });

// Print each document in a readable format
documents.forEach(function(doc) {
    printjson(doc);
});
