// Get the database name from command line
var dbName = db.getMongo().getDBName();

// Switch to the specified database (already done implicitly)
use dbName;

// Create a document to insert
var document = {
    name: "Holberton school"
};

// Insert the document into the 'school' collection
db.school.insertOne(document);
